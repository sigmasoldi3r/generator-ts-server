# basic config
host:
  port: 8080
  name: localhost
  protocol: http

database:
  type: <%= dbConfig %>
  host: <%= host %>
  port: <%= port %>
  username: <%= user %>
  password: <%= password %>
  database: <%= name %>
  synchronize: true
  logging: false

# Remove this, sample extra config
sample:
  title: A full node backend
